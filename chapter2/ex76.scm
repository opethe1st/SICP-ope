;; if more types would be added more frequently then message passing is the way to go.
;; If more functions would be added, data-directed programming or generic operations with explicit dispatch would be more appropriate
